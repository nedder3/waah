{
  "name": "waah",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "We Have AWS At Home — emulador client-side de servicios AWS en el navegador (GitHub Pages, sin build).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node server.mjs",
    "verify": "hermes verify"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.6.0"
  }
}
